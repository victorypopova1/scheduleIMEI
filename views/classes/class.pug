extends ../layout
block variables
  include ../variables
block content
  if username!=''
    h1= title
    form(action='/class/'+val.id, method='post')
      input(name='name', class="form-control", value=val.name)
      input(type='submit',class="btn", value='Изменить')
      input(type='submit', class="btn", value='Отменить')